# 2026-03-28 日志

## 本次修复内容（代码审查后的问题修复）

### tools.html
- 修复简悦链接：`book.sohu.com` → `https://ksria.com/simpread`，`data-name` 从 `读书笔记` 改为 `简悦`
- 替换灵感发现区重复的「少数派」为「即刻（web.okjike.com）」
- 添加 `scroll-padding-top:80px`，修正 hash 跳转被 nav 遮挡
- Modal 打开/关闭时锁定/解锁 `body.overflow`，防止背景滚动

### index.html
- 平滑滚动偏移量从 60 → 80，匹配 nav 高度
- footer 社交链接从通用首页改为 `space.bilibili.com/` 等个人主页格式，并加注释提示替换

### style.css
- 添加 `scroll-padding-top:80px`，修正锚点跳转偏移
- `nav.scrolled .nav-links a:hover` 改为 accent 色，增加 hover 视觉反馈
- 新增 `@media(max-width:960px)` 断点，works 卡片中等屏幕改为 2 列

### knowledge.html
- `showWelcome()` 添加崩溃保护：过滤 docs 为空的分类，onclick 改为事件委托
- `renderDoc()` 添加 marked 未加载时的降级处理（显示原始 markdown）
- 加载失败提示改善：加入"请运行 start.bat"的说明
- breadcrumb / error 中的用户输入改用 `escHtml()` 转义，防 XSS
