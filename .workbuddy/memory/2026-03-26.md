# 2026-03-26 日志

## 网站部署与布局调整

- 用户成功将网站部署到 Vercel，地址：https://yunfei-web.vercel.app
- 解答了 Vercel 注册选 Hobby 套餐、GitHub 仓库权限选 Only select repositories
- 解释了 vercel.app 域名在国内偶尔打不开的原因（DNS/被墙），建议绑定自定义域名
- 确认代码已有移动端适配（max-width:680px 断点）

## 布局修改

- `style.css`：`my-works-grid` 从 2列(repeat(2,1fr)) 改为 4列(repeat(4,1fr))，max-width 从 720px 改为 1100px；缩略图高度从 154px 改为 120px；移动端改为 2列
- `works.html`：`.wgrid` 从 auto-fill(minmax(260px,1fr)) 改为 repeat(4,1fr)；移动端改为 2列
