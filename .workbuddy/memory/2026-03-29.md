# 2026-03-29 日志

## 知识库界面改造（语雀/飞书风格）
- 主色调改为橙色 #e8632a（语雀橙）
- 背景全白，侧边栏浅米灰 #faf9f7
- 侧边栏文章条目：平铺 + 左边框激活高亮（橙色），无小圆点
- h2：橙色左边框 + 浅橙背景，h3：橙色竖线前缀
- 代码块：浅灰底 + 语言标签（JS插入DOM），行内代码橙色调
- 顶部导航加动态面包屑（打开文章时更新）
- 欢迎页卡片 hover 改为橙色阴影
- 深色模式同步适配橙色主题

- server.js 重构：
  - 启动时同步预热缓存（scanDocs 同步执行），之后 /api/docs 直接返回内存数据，无需每次扫描
  - 用 fs.watch 监听 docs/ 目录变化，自动防抖（300ms）重建缓存
  - 启动时自动生成/更新 docs/_sidebar.json（静态 fallback，静态部署也能用）
  - 图片/视频文件加 Cache-Control: max-age=86400，减少重复请求
## 知识库大纲+拖拽+飞书样式补全

- 侧边栏新增文档内大纲功能：点开文章后，自动在文章条目下展示 h2/h3 大纲，可点击跳转，滚动时高亮当前标题
- 分类和文章均支持拖拽排序，顺序持久化到 localStorage（key: kb_order），页面刷新后保留
- 拖拽 handle（⠿）hover 时浮现，不影响点击交互
- h1：补加底部分隔线，font-size 2rem，飞书风格
- h2：去掉背景色，保留左侧橙色细边框，间距优化
- h3：加前置小圆点（accent-sub 橙色），更有层次感
- h4：颜色调整为 text2

- 2026-03-29：知识库大纲改为默认折叠，文章条目左侧增加 ▶ 箭头，点箭头才展开/折叠大纲，与飞书交互一致
  - loadTree() 加 sessionStorage 缓存（key: kb_tree_cache）
  - 有缓存时先立即渲染，再后台静默刷新；首次加载也因服务端预热变快
  - 去掉所有 ?t=Date.now() 缓存破坏参数，让浏览器正常缓存 MD 文件

## 知识库 4 项修复（09:54）
- 拖拽排序：重写为容器级事件委托（mousedown记录handle标记，dragstart捕获阶段判断，解决上往下拖失效 bug）
- 折叠箭头（sb-doc-arrow）改为常显灰色，无大纲时 transparent+pointer-events:none
- 侧边栏字体颜色调深：分类标题 text3→text2，文章条目 text2→text
- meta 区：font-size .73rem→.82rem，color text3→text2，font-weight:500；新增修改日期（🕓 YYYY-MM-DD）
- server.js scanDocs 增加 mtime: fs.statSync(filePath).mtimeMs，前端 docObj.mtime 格式化展示

- 重构 renderOutline：h2 节点带独立展开箭头（▶），默认折叠，点箭头展开旗下 h3
- h3 默认隐藏（`.sb-outline-h3`），h2 open 后才 visible
- 无父 h2 的孤立 h3 加 top-level，直接平铺显示
- h2 下无 h3 子节点的加 no-children，箭头不可点（visibility:hidden）
- setupOutlineScroll 改为 [data-target] 选择器，兼容新类名
- 整体逻辑：点文章旁 ▶ 展开大纲面板 → 面板内 h2 行再各自独立展开 h3

## 大纲缩进再调整（10:17）
- 文章条目 padding-left: 1.6rem（第1级）
- 大纲 h2 padding-left: 3.0rem（第2级，+1.4rem）
- 大纲 h3 padding-left: 4.2rem（第3级，+1.2rem）
- top-level h3（无父h2）padding-left: 3.0rem 与 h2 对齐


- 默认展开所有分类（改为 querySelectorAll('.sb-cat-header').forEach(h => openCat(h))）
- 大纲区域去掉左侧竖线，改为纯缩进（h2: padding-left 2.2rem，h3: 3.4rem，top-level h3: 2.2rem）
- 删除分类标题右侧文章数字（cat-count span 和 CSS 均删除）


- 分类（.sb-cat）间加 border-top 分隔线，margin-bottom 加大，视觉上彼此分组
- 文章（.sb-doc-item）padding-left 从 1.1rem 增至 1.6rem，更明显内缩
- 大纲（.sb-outline.open）改为 margin-left:2.4rem + border-left 细线，整体内缩附属于文章
- 大纲 h2 缩进改为 padding-left:.75rem（相对大纲容器），h3 缩进 1.9rem
- 纯 h3（top-level）padding-left:.75rem 与 h2 对齐
- sb-doc-arrow（文章条目左侧箭头）：hover 文章条目才显示，其余时间透明
- h2-arrow（大纲 h2 行箭头）：hover h2 行才显示，open 状态 hover 显示橙色
- sb-cat-header .arrow（分类标题箭头）：hover 分类标题才显示，其余时间透明
- 三处箭头统一规则：默认 color:transparent，鼠标悬停才可见

## 侧边栏&欢迎页样式调整（10:30）
- 欢迎页卡片 minmax 从 190px 改为 150px，4个分类卡片可一行显示
- 侧边栏分类间去掉 border-top 分隔线，改为 margin-top:1.1rem 留白区分，视觉更简洁

## 知识库改为纯静态模式（10:22）
- loadTree() 重写：不再请求 /api/docs，直接 fetch docs/_sidebar.json
- 新增 FALLBACK_TREE 内嵌数据：file:// 协议直接打开时 fetch 也失败的最终兜底
- _sidebar.json 去掉 mtime 字段，保持简洁，与实际 docs 目录同步
- server.js 和 start.bat 仍保留，本地开发时可用，但非必需

## 前端教程文档入库 + 样式调整

- 新建 docs/web/ 目录，新增三篇前端教程 MD：
  - html-tutorial.md（HTML 基础到HTML5，含表单、语义化、多媒体等12章）
  - css-tutorial.md（CSS 基础到响应式，含Flexbox/Grid/动画等14章）
  - js-tutorial.md（JS 基础到ES6+，含DOM/事件/异步/Fetch等14章）
- _sidebar.json 新增 "web"（前端开发）分类，排在最前
- knowledge.html：
  - CAT_ICONS 添加 web: '🌐'
  - h2 样式改为简洁左边框（去掉背景色）
  - h3 样式改为简洁加粗（去掉竖线伪元素）
  - .doc-wrap 去掉 margin:0 auto，改为居左显示
